import { FC } from 'react';
import { MainContainer } from '../../../components/main-content/main-content';

interface ITicketsPage {}

export const TicketsPage: FC<ITicketsPage> = () => (
  <MainContainer>TicketsPage</MainContainer>
);
