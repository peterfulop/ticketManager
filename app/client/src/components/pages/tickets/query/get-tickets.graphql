query GetMyTickets($ticketSearchParams: SearchTicketInput) {
  getMyTickets(input: $ticketSearchParams) {
    userErrors {
      message
      values
    }
    tickets {
      id
      projectId
      title
      comment
      priority
      status
      references
      createdAt
      updatedAt
    }
  }
}
