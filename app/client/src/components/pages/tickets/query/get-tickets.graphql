query GetMyTickets($ticketSearchParams: SearchTicketInput) {
  getMyTickets(input: $ticketSearchParams) {
    userErrors {
      message
      values
    }
    tickets {
      id
      projectId
      title
      comment
      priority
      type
      status
      references
      storyPoints
      createdAt
      updatedAt
    }
  }
}
