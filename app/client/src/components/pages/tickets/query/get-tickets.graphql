query GetMyTickets {
  getMyTickets {
    userErrors {
      message
      values
    }
    tickets {
      id
      projectId
      title
      comment
      priority
      status
      references
      createdAt
      updatedAt
    }
  }
}
